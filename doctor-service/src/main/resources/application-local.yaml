server:
  port: 8082

spring:
  data:
    mongodb:
      uri: mongodb://user1:34525@localhost:27017/clinic

springdoc:
  api-docs:
    enabled: true
    path: /doctor-service/v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus